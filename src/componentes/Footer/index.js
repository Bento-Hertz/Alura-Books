import styled from 'styled-components';

const FooterContainer = styled.footer`
    background: black;
    display: flex;
    heigth: 200px;
`

function Footer() {
    return(
        <FooterContainer><p>affgs</p></FooterContainer>
    );
}

export default Footer;